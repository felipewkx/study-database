-- DROP SCHEMA clinica_pi_uc6;

CREATE SCHEMA clinica_pi_uc6 AUTHORIZATION postgres;

-- DROP SEQUENCE clinica_pi_uc6.consulta_id_consulta_seq;

CREATE SEQUENCE clinica_pi_uc6.consulta_id_consulta_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 9223372036854775807
	START 1
	CACHE 1
	NO CYCLE;
-- DROP SEQUENCE clinica_pi_uc6.consultorio_id_consultorio_seq;

CREATE SEQUENCE clinica_pi_uc6.consultorio_id_consultorio_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 2147483647
	START 1
	CACHE 1
	NO CYCLE;
-- DROP SEQUENCE clinica_pi_uc6.convenio_id_convenio_seq;

CREATE SEQUENCE clinica_pi_uc6.convenio_id_convenio_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 2147483647
	START 1
	CACHE 1
	NO CYCLE;
-- DROP SEQUENCE clinica_pi_uc6.especialidade_id_especialidade_seq;

CREATE SEQUENCE clinica_pi_uc6.especialidade_id_especialidade_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 2147483647
	START 1
	CACHE 1
	NO CYCLE;
-- DROP SEQUENCE clinica_pi_uc6.paciente_id_paciente_seq;

CREATE SEQUENCE clinica_pi_uc6.paciente_id_paciente_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 2147483647
	START 1
	CACHE 1
	NO CYCLE;
-- DROP SEQUENCE clinica_pi_uc6.profissional_id_profissional_seq;

CREATE SEQUENCE clinica_pi_uc6.profissional_id_profissional_seq
	INCREMENT BY 1
	MINVALUE 1
	MAXVALUE 2147483647
	START 1
	CACHE 1
	NO CYCLE;-- clinica_pi_uc6.consultorio definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.consultorio;

CREATE TABLE clinica_pi_uc6.consultorio (
	id_consultorio int4 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL,
	nome varchar(50) NOT NULL,
	andar int2 NOT NULL,
	capacidade int2 DEFAULT 1 NOT NULL,
	ativo bool DEFAULT true NOT NULL,
	CONSTRAINT ck_consultorio_andar CHECK (((andar >= 1) AND (andar <= 10))),
	CONSTRAINT ck_consultorio_capacidade CHECK ((capacidade > 0)),
	CONSTRAINT consultorio_nome_key UNIQUE (nome),
	CONSTRAINT consultorio_pkey PRIMARY KEY (id_consultorio)
);


-- clinica_pi_uc6.convenio definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.convenio;

CREATE TABLE clinica_pi_uc6.convenio (
	id_convenio int4 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL,
	nome varchar(80) NOT NULL,
	tipo varchar(20) NOT NULL,
	ativo bool DEFAULT true NOT NULL,
	CONSTRAINT ck_convenio_tipo CHECK (((tipo)::text = ANY ((ARRAY['PARTICULAR'::character varying, 'SAUDE'::character varying, 'EMPRESARIAL'::character varying])::text[]))),
	CONSTRAINT convenio_nome_key UNIQUE (nome),
	CONSTRAINT convenio_pkey PRIMARY KEY (id_convenio)
);


-- clinica_pi_uc6.especialidade definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.especialidade;

CREATE TABLE clinica_pi_uc6.especialidade (
	id_especialidade int4 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL,
	nome varchar(80) NOT NULL,
	descricao varchar(255) NOT NULL,
	CONSTRAINT especialidade_nome_key UNIQUE (nome),
	CONSTRAINT especialidade_pkey PRIMARY KEY (id_especialidade)
);


-- clinica_pi_uc6.paciente definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.paciente;

CREATE TABLE clinica_pi_uc6.paciente (
	id_paciente int4 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL,
	nome varchar(120) NOT NULL,
	data_nascimento date NOT NULL,
	sexo bpchar(1) NOT NULL,
	cidade varchar(80) NOT NULL,
	id_convenio int4 NOT NULL,
	data_cadastro date NOT NULL,
	ativo bool DEFAULT true NOT NULL,
	CONSTRAINT ck_paciente_nascimento CHECK ((data_nascimento < CURRENT_DATE)),
	CONSTRAINT ck_paciente_sexo CHECK ((sexo = ANY (ARRAY['F'::bpchar, 'M'::bpchar, 'X'::bpchar]))),
	CONSTRAINT paciente_pkey PRIMARY KEY (id_paciente),
	CONSTRAINT paciente_id_convenio_fkey FOREIGN KEY (id_convenio) REFERENCES clinica_pi_uc6.convenio(id_convenio)
);
CREATE INDEX idx_paciente_convenio ON clinica_pi_uc6.paciente USING btree (id_convenio);


-- clinica_pi_uc6.profissional definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.profissional;

CREATE TABLE clinica_pi_uc6.profissional (
	id_profissional int4 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 2147483647 START 1 CACHE 1 NO CYCLE) NOT NULL,
	nome varchar(120) NOT NULL,
	registro_profissional varchar(30) NOT NULL,
	id_especialidade int4 NOT NULL,
	data_admissao date NOT NULL,
	ativo bool DEFAULT true NOT NULL,
	CONSTRAINT profissional_pkey PRIMARY KEY (id_profissional),
	CONSTRAINT profissional_registro_profissional_key UNIQUE (registro_profissional),
	CONSTRAINT profissional_id_especialidade_fkey FOREIGN KEY (id_especialidade) REFERENCES clinica_pi_uc6.especialidade(id_especialidade)
);
CREATE INDEX idx_profissional_especialidade ON clinica_pi_uc6.profissional USING btree (id_especialidade);


-- clinica_pi_uc6.consulta definition

-- Drop table

-- DROP TABLE clinica_pi_uc6.consulta;

CREATE TABLE clinica_pi_uc6.consulta (
	id_consulta int8 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START 1 CACHE 1 NO CYCLE) NOT NULL,
	id_paciente int4 NOT NULL,
	id_profissional int4 NOT NULL,
	id_consultorio int4 NOT NULL,
	data_agendamento timestamp NOT NULL,
	data_hora_consulta timestamp NOT NULL,
	status varchar(15) NOT NULL,
	tipo_atendimento varchar(20) NOT NULL,
	valor_cobrado numeric(10, 2) NULL,
	inicio_atendimento timestamp NULL,
	fim_atendimento timestamp NULL,
	observacao varchar(255) NULL,
	CONSTRAINT ck_consulta_agendamento CHECK ((data_agendamento <= data_hora_consulta)),
	CONSTRAINT ck_consulta_periodo CHECK ((((inicio_atendimento IS NULL) AND (fim_atendimento IS NULL)) OR ((inicio_atendimento IS NOT NULL) AND (fim_atendimento IS NOT NULL) AND (fim_atendimento >= inicio_atendimento)))),
	CONSTRAINT ck_consulta_status CHECK (((status)::text = ANY ((ARRAY['AGENDADA'::character varying, 'REALIZADA'::character varying, 'FALTOU'::character varying, 'CANCELADA'::character varying])::text[]))),
	CONSTRAINT ck_consulta_tipo CHECK (((tipo_atendimento)::text = ANY ((ARRAY['PRESENCIAL'::character varying, 'TELECONSULTA'::character varying])::text[]))),
	CONSTRAINT consulta_pkey PRIMARY KEY (id_consulta),
	CONSTRAINT consulta_id_consultorio_fkey FOREIGN KEY (id_consultorio) REFERENCES clinica_pi_uc6.consultorio(id_consultorio),
	CONSTRAINT consulta_id_paciente_fkey FOREIGN KEY (id_paciente) REFERENCES clinica_pi_uc6.paciente(id_paciente),
	CONSTRAINT consulta_id_profissional_fkey FOREIGN KEY (id_profissional) REFERENCES clinica_pi_uc6.profissional(id_profissional)
);
CREATE INDEX idx_consulta_paciente ON clinica_pi_uc6.consulta USING btree (id_paciente);
CREATE INDEX idx_consulta_profissional_data ON clinica_pi_uc6.consulta USING btree (id_profissional, data_hora_consulta);
CREATE INDEX idx_consulta_status_data ON clinica_pi_uc6.consulta USING btree (status, data_hora_consulta);